import 'package:flutter/material.dart';

class CustomColor {
  // Màu chủ đạo
  static const Color secondBlue = Color(0xFF012E4A); // Changed to #012E4A
  static const Color lightBlue = Color(0xFF036280); // Changed to #036280
  static const Color white = Color(0xFFFFFFFF); // Trắng
  static const Color lightGray = Color(0xFFF5F5F5);

  static const Color primaryBlue = Color(0xFFE6EFF2);
  static const Color softCyan = Color(0xFFD0E1E7);
  // Màu nhấn
  static const Color softGreen = Color(0xFFC8D0BC);
  static const Color grayLightBackground = Color(0xFFF5F5F5);
  // Màu phụ trợ
  static const Color textDarkGray =
      Color(0xFF424242); // Màu chữ chính (unchanged)
  static const Color coolGray = Color(0xFF036280); // Changed to #036280

  // Màu cũ giữ lại
  static const Color primaryRed =
      Color(0xFFD10023); // Cảnh báo hoặc nhấn mạnh (unchanged)
  static const Color pastelOrange =
      Color(0xFFFFCC80); // Phần tử tương tác (unchanged)

  // Màu mới cho tiêu đề và văn bản
  static const Color deepBlue = Color(0xFF012E4A); // Changed to #012E4A
  static const Color deepGreen = Color(0xFF036280); // Changed to #036280
  static const Color warmBrown =
      Color(0xFF795548); // Nâu ấm cho tiêu đề phụ (unchanged)
  static const Color neutralGray =
      Color(0xFF757575); // Xám trung tính cho văn bản phụ (unchanged)
}
